package edu.ucentral.servicio.patients.clients;
/*
import org.springframework.cloud.openfeign.FeignClient;
import org.springframework.web.bind.annotation.GetMapping;
import org.springframework.web.bind.annotation.PathVariable;
*/
//@FeignClient(name = "servicio-beds") // nombre con el microservicio que se va a comunicar
public interface AnswerFeignClient {

	// EndPoint con el que se va a comunicar
	//@GetMapping("/patient/{patientId}/camas-asignadas")
	//public Iterable<Long> obtenerCamasAsignadasAPaciente(@PathVariable Long patientId);

}
